# 测试用例描述 Tests.md

## 项目：Web Agent Chat System

---

## 1. 后端测试

### 1.1 数据库层测试

#### TC-DB-01: 数据库初始化
| 用例编号 | TC-DB-01 |
|---------|----------|
| 测试内容 | 初始化 SQLite 数据库并创建表结构 |
| 前置条件 | 无 |
| 测试步骤 | 1. 调用 database.init() <br>2. 检查 sessions/messages/settings 表是否存在 |
| 预期结果 | 所有表创建成功，无错误 |

#### TC-DB-02: 创建会话
| 用例编号 | TC-DB-02 |
|---------|----------|
| 测试内容 | 向 sessions 表插入新会话 |
| 前置条件 | 数据库已初始化 |
| 测试步骤 | 1. 调用 createSession(title) <br>2. 查询该会话记录 |
| 预期结果 | 返回的会话对象包含 id/title/createdAt，且数据库中存在对应记录 |

#### TC-DB-03: 创建消息
| 用例编号 | TC-DB-03 |
|---------|----------|
| 测试内容 | 向 messages 表插入消息 |
| 前置条件 | 会话已存在 |
| 测试步骤 | 1. 调用 createMessage(sessionId, role, content) <br>2. 查询该消息记录 |
| 预期结果 | 返回的消息对象包含正确内容，且关联到对应会话 |

#### TC-DB-04: 获取会话消息列表
| 用例编号 | TC-DB-04 |
|---------|----------|
| 测试内容 | 获取指定会话的所有消息 |
| 前置条件 | 会话包含多条消息 |
| 测试步骤 | 调用 getMessagesBySession(sessionId) |
| 预期结果 | 返回按 createdAt 排序的消息数组 |

#### TC-DB-05: 删除会话
| 用例编号 | TC-DB-05 |
|---------|----------|
| 测试内容 | 删除会话及其关联消息 |
| 前置条件 | 会话存在 |
| 测试步骤 | 1. 调用 deleteSession(sessionId) <br>2. 尝试查询会话和消息 |
| 预期结果 | 会话和消息均已删除 |

#### TC-DB-06: 读取/保存设置
| 用例编号 | TC-DB-06 |
|---------|----------|
| 测试内容 | 读取和保存设置 |
| 前置条件 | 无 |
| 测试步骤 | 1. 调用 saveSettings(settings) <br>2. 调用 getSettings() |
| 预期结果 | 返回的设置与保存的一致 |

---

### 1.2 REST API 测试

#### TC-API-01: 获取会话列表
| 用例编号 | TC-API-01 |
|---------|----------|
| 测试内容 | GET /api/sessions |
| 前置条件 | 数据库包含多个会话 |
| 测试步骤 | 发送 GET 请求到 /api/sessions |
| 预期结果 | 返回 200 + 会话数组 |

#### TC-API-02: 创建新会话
| 用例编号 | TC-API-02 |
|---------|----------|
| 测试内容 | POST /api/sessions |
| 前置条件 | 无 |
| 测试步骤 | 发送 POST 请求，body: { title: "新会话" } |
| 预期结果 | 返回 201 + 新会话对象 |

#### TC-API-03: 获取会话详情
| 用例编号 | TC-API-03 |
|---------|----------|
| 测试内容 | GET /api/sessions/:id |
| 前置条件 | 会话存在 |
| 测试步骤 | 发送 GET 请求到 /api/sessions/{id} |
| 预期结果 | 返回 200 + 会话对象（含 messages 数组） |

#### TC-API-04: 删除会话
| 用例编号 | TC-API-04 |
|---------|----------|
| 测试内容 | DELETE /api/sessions/:id |
| 前置条件 | 会话存在 |
| 测试步骤 | 发送 DELETE 请求到 /api/sessions/{id} |
| 预期结果 | 返回 204，查询该会话返回 404 |

#### TC-API-05: 发送消息（非流式）
| 用例编号 | TC-API-05 |
|---------|----------|
| 测试内容 | POST /api/chat |
| 前置条件 | LLM 配置正确 |
| 测试步骤 | 发送 POST 请求，body: { sessionId, content: "你好" } |
| 预期结果 | 返回 200 + AI 回复内容 |

#### TC-API-06: 发送消息（流式）
| 用例编号 | TC-API-06 |
|---------|----------|
| 测试内容 | POST /api/chat (streaming: true) |
| 前置条件 | LLM 配置正确 |
| 测试步骤 | 发送 POST 请求，stream: true |
| 预期结果 | 返回 text/event-stream，分块返回内容 |

#### TC-API-07: 导出会话
| 用例编号 | TC-API-07 |
|---------|----------|
| 测试内容 | GET /api/export/:id |
| 前置条件 | 会话存在且包含消息 |
| 测试步骤 | 发送 GET 请求到 /api/export/{id} |
| 预期结果 | 返回 JSON 文件下载，包含完整会话信息 |

#### TC-API-08: 导入会话
| 用例编号 | TC-API-08 |
|---------|----------|
| 测试内容 | POST /api/import |
| 前置条件 | 有效的会话 JSON |
| 测试步骤 | 发送 POST 请求，body: { json: "..." } |
| 预期结果 | 返回 201 + 新会话对象（id 不同） |

#### TC-API-09: 获取/保存设置
| 用例编号 | TC-API-09 |
|---------|----------|
| 测试内容 | GET/PUT /api/settings |
| 前置条件 | 无 |
| 测试步骤 | 1. PUT /api/settings 更新设置 <br>2. GET /api/settings 获取设置 |
| 预期结果 | GET 返回更新后的设置 |

---

### 1.3 LLM 服务测试

#### TC-LLM-01: LLM 同步调用
| 用例编号 | TC-LLM-01 |
|---------|----------|
| 测试内容 | 调用 OpenAI 兼容接口获取回复 |
| 前置条件 | LLM 配置正确，API 可用 |
| 测试步骤 | 调用 llm.chat(config, messages) |
| 预期结果 | 返回完整的 AI 回复内容 |

#### TC-LLM-02: LLM 流式调用
| 用例编号 | TC-LLM-02 |
|---------|----------|
| 测试内容 | 调用 LLM 并获取流式响应 |
| 前置条件 | LLM 配置正确，API 支持流式 |
| 测试步骤 | 调用 llm.chatStream(config, messages) |
| 预期结果 | 返回 ReadableStream，分块返回内容 |

#### TC-LLM-03: System Prompt 注入
| 用例编号 | TC-LLM-03 |
|---------|----------|
| 测试内容 | System Prompt 正确注入到消息数组 |
| 前置条件 | 设置了 System Prompt |
| 测试步骤 | 构建消息时检查第一条是否为 system 消息 |
| 预期结果 | 第一条消息 role 为 system，content 为配置的 System Prompt |

#### TC-LLM-04: LLM 调用错误处理
| 用例编号 | TC-LLM-04 |
|---------|----------|
| 测试内容 | API Key 错误或网络异常时正确报错 |
| 前置条件 | API Key 错误 |
| 测试步骤 | 调用 llm.chat(config, messages) |
| 预期结果 | 抛出错误，包含错误信息 |

---

### 1.4 GraphQL 测试

#### TC-GQL-01: 查询会话列表
| 用例编号 | TC-GQL-01 |
|---------|----------|
| 测试内容 | sessions 查询 |
| 前置条件 | 数据库包含会话 |
| 测试步骤 | 执行 GraphQL 查询 { sessions { id title } } |
| 预期结果 | 返回会话数组 |

#### TC-GQL-02: 查询单个会话
| 用例编号 | TC-GQL-02 |
|---------|----------|
| 测试内容 | session 查询 |
| 前置条件 | 会话存在 |
| 测试步骤 | 执行 GraphQL 查询 { session(id: "xxx") { id title messages { content } } } |
| 预期结果 | 返回会话及其消息 |

#### TC-GQL-03: 创建会话
| 用例编号 | TC-GQL-03 |
|---------|----------|
| 测试内容 | createSession 突变 |
| 前置条件 | 无 |
| 测试步骤 | 执行 GraphQL 突变 { createSession(title: "test") { id title } } |
| 预期结果 | 返回新会话对象 |

#### TC-GQL-04: 删除会话
| 用例编号 | TC-GQL-04 |
|---------|----------|
| 测试内容 | deleteSession 突变 |
| 前置条件 | 会话存在 |
| 测试步骤 | 执行 GraphQL 突变 { deleteSession(id: "xxx") } |
| 预期结果 | 返回 true |

#### TC-GQL-05: 更新设置
| 用例编号 | TC-GQL-05 |
|---------|----------|
| 测试内容 | updateSettings 突变 |
| 前置条件 | 无 |
| 测试步骤 | 执行 GraphQL 突变 { updateSettings(input: { apiKey: "xxx" }) { apiKey } } |
| 预期结果 | 返回更新后的设置 |

---

## 2. 前端测试

### 2.1 组件测试

#### TC-UI-01: SessionList 渲染
| 用例编号 | TC-UI-01 |
|---------|----------|
| 测试内容 | 正确渲染会话列表 |
| 前置条件 | 有多个会话 |
| 测试步骤 | 渲染 SessionList 组件 |
| 预期结果 | 显示所有会话标题，点击选中当前会话 |

#### TC-UI-02: MessageList 渲染
| 用例编号 | TC-UI-02 |
|---------|----------|
| 测试内容 | 正确渲染消息列表 |
| 前置条件 | 会话包含消息 |
| 测试步骤 | 渲染 MessageList 组件 |
| 预期结果 | 用户消息在右，AI 消息在左，按时间顺序排列 |

#### TC-UI-03: 代码高亮
| 用例编号 | TC-UI-03 |
|---------|----------|
| 测试内容 | AI 回复中的代码块正确高亮 |
| 前置条件 | AI 回复包含代码块 |
| 测试步骤 | 渲染包含代码的消息 |
| 预期结果 | 代码块使用 highlight.js 正确着色 |

#### TC-UI-04: 复制功能
| 用例编号 | TC-UI-04 |
|---------|----------|
| 测试内容 | 点击复制按钮复制消息内容 |
| 前置条件 | 消息内容非空 |
| 测试步骤 | 点击复制按钮 |
| 预期结果 | 内容复制到剪贴板，无报错 |

#### TC-UI-05: 文件上传
| 用例编号 | TC-UI-05 |
|---------|----------|
| 测试内容 | 选择文件后显示文件名 |
| 前置条件 | 无 |
| 测试步骤 | 在 InputArea 选择文件 |
| 预期结果 | 文件名显示在输入框附近 |

#### TC-UI-06: 主题切换
| 用例编号 | TC-UI-06 |
|---------|----------|
| 测试内容 | 切换主题后界面配色变化 |
| 前置条件 | 当前为 light 主题 |
| 测试步骤 | 点击切换主题按钮 |
| 预期结果 | 界面变为 dark 主题配色 |

### 2.2 Store 测试

#### TC-STORE-01: session store 创建会话
| 用例编号 | TC-STORE-01 |
|---------|----------|
| 测试内容 | Pinia session store 创建新会话 |
| 前置条件 | 无 |
| 测试步骤 | 调用 sessionStore.createSession(title) |
| 预期结果 | 新会话添加到 sessions 数组，currentSessionId 更新 |

#### TC-STORE-02: session store 删除会话
| 用例编号 | TC-STORE-02 |
|---------|----------|
| 测试内容 | 删除会话并更新状态 |
| 前置条件 | 存在多个会话 |
| 测试步骤 | 调用 sessionStore.deleteSession(id) |
| 预期结果 | 会话从 sessions 数组移除，若删除当前会话则切换到其他会话 |

#### TC-STORE-03: chat store 发送消息
| 用例编号 | TC-STORE-03 |
|---------|----------|
| 测试内容 | 发送消息并更新消息列表 |
| 前置条件 | 已选择会话 |
| 测试步骤 | 调用 chatStore.sendMessage(content) |
| 预期结果 | 用户消息添加到消息列表，AI 回复追加到列表 |

#### TC-STORE-04: chat store 流式响应
| 用例编号 | TC-STORE-04 |
|---------|----------|
| 测试内容 | 流式响应时逐块更新消息 |
| 前置条件 | API 配置支持流式 |
| 测试步骤 | 调用 chatStore.sendMessage(content) |
| 预期结果 | streaming 为 true，消息内容随流式更新 |

#### TC-STORE-05: settings store 主题切换
| 用例编号 | TC-STORE-05 |
|---------|----------|
| 测试内容 | 切换主题并持久化 |
| 前置条件 | 当前为 light |
| 测试步骤 | 调用 settingsStore.setTheme('dark') |
| 预期结果 | theme 变为 'dark'，下次页面加载保持 dark |

---

## 3. 集成测试

### 3.1 端到端流程测试

#### TC-E2E-01: 完整聊天流程
| 用例编号 | TC-E2E-01 |
|---------|----------|
| 测试内容 | 从创建会话到发送消息的完整流程 |
| 前置条件 | 后端运行中，LLM 配置正确 |
| 测试步骤 | 1. 新建会话 <br>2. 输入 "你好" <br>3. 等待 AI 回复 |
| 预期结果 | 消息显示在聊天区，AI 回复正确显示 |

#### TC-E2E-02: 多轮对话
| 用例编号 | TC-E2E-02 |
|---------|----------|
| 测试内容 | 多轮连续对话，上下文保持 |
| 前置条件 | 已完成 TC-E2E-01 |
| 测试步骤 | 1. 发送 "介绍一下你自己" <br>2. 发送 "再详细一点" |
| 预期结果 | 两轮对话都显示，AI 能理解上下文 |

#### TC-E2E-03: 会话导出导入
| 用例编号 | TC-E2E-03 |
|---------|----------|
| 测试内容 | 导出会话后重新导入 |
| 前置条件 | 有会话包含消息 |
| 测试步骤 | 1. 导出会话 <br>2. 删除该会话 <br>3. 导入 JSON |
| 预期结果 | 新会话包含原有消息 |

#### TC-E2E-04: 主题持久化
| 用例编号 | TC-E2E-04 |
|---------|----------|
| 测试内容 | 刷新页面后主题保持 |
| 前置条件 | 已切换到 dark 主题 |
| 测试步骤 | 刷新浏览器页面 |
| 预期结果 | 页面仍为 dark 主题 |

---

## 4. 测试覆盖矩阵

| 模块 | 单元测试 | 集成测试 | E2E |
|------|----------|----------|-----|
| 数据库层 | ✓ | - | - |
| REST API | - | ✓ | - |
| LLM 服务 | ✓ | - | - |
| GraphQL | - | ✓ | - |
| 前端组件 | ✓ | - | - |
| Pinia Store | ✓ | - | - |
| 完整流程 | - | - | ✓ |