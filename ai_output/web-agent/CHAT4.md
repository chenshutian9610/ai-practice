---
功能: 开发者模式
环境: claudecode + minimax-m2.5
工具:
  - playwright-cli
  - superpowers
    - /superpowers:brainstorm 
    - /superpowers:write-plan
---

```
对话记录在 /compact 后丢了
```

```
你修复后通过调用后台接口自行测试, 测试用例: 你有多少工具, 期望结果, 含有获取当前时间的工具
```

```
功能有问题, 测试用例: 现在几点, 期望结果, 正确返回时间. 
你修复后通过调用后台接口自行测试
```

```
如果你确定后台是没问题了, 就使用 /playwright-cli 看下前端的效果, ng 的
```

minimax 读不了图片... 所以截图跟它说问题这一步是错的, 即使使用 playwright, 也只是读终端的报错

```
如果存在工具调用, 那么过程应该是
1. 模型返回要调用的工具
2. MCP 主机调用工具
3. 模型拿到工具调用的结果, 判断是否还需要调用工具, 或是直接返回 (这一步明显没有)
```